-Data types in java
    primitive-byte,short,char,boolean,int,long,flat,double
    non-primitive-string,array,class,object and interface
-java is typed n strict language
-cuz of sizes in memory we decleare data types in java
    short -take 2 bytes
    char-take 2 bytes
-//(single line code) and /* */(multiple commands)

-Input in java  
    next
    nextline(jb hamme space bhi store krane ho)
    nextLint
    NextFloat
    nextByte
    nextDouble
    nextBoolean
    nextShort
    nextLong

    -Scanner is class and we write
    Scanner sc=newscanner(System.in);
        but for this we write=import java.util.*;
        *sc.close() last mei likhna padhta hai

-3.14f=ham floting value ko number k baad f lagate kyuki tb java usko
        double declare kr deta
            ex.= float area_of_circle=3.14f*radius*radius


-TYPE CONVERSION
    conersion happens when:
        a.type compatible
        b.destination tpe > source type
*byte->short->int->float->long->double
    we can write int a;
                long b=a;    (widening/implicit conversion)
    we can write long a;
                int b=a;     (its lossy conversion)


-TYPE CASTING  (narrowing/explicit conversion)
    suppose, float a=25.0;
    we type, int b=a;           (show error)
    but if , int b=(int)a;      [type casting]

*type conversion sirf expression k sath hota like "a-b*c"
byte b=5;
byte a= b*2;  (it shows error cuz of java convert iT into int)
b=(byte)(b*2); (right)

*Types of operators
    arithmetic operators(binary/unary(++,--))(++a refes to a=a+1)
        (a++ refes to pehle add tb samew value of a in b)
    relational operators(==,>=,=!,<=)
    logical operators(&&,||,!)
    BITWISE operators
    assignment operators(+=,*=,/=)

*Conditional statment
    if-else

-ternary operators
    variable=condition?statement1:statement2
        : refers nahi toh

-SWITCH statement1
    Syntax:switch(variable){
        case1:
            break;
        case2:
            break;
        case3:    
            break;   (case ki value compare hokr true de de)
        default:
        (agar ek statement true hota baki k sab true ho jaate )
            to stop this we use "break"
    }


-WHILE LOOP
    while(condition){
        //do something
    }   (pehle integer ki value do )

*infinite loops
    while(true){    (jb tak memory na bhar jaye)
        //do something
    }

-FOR LOOP
    for(initialisation;condition;updation){
        //do something
    }
-do-while loop
    do{
        //dosomething
    }while(condition);




-FUNCTIONS
    *-*Sntax=
        returnType name(){
            //body
            return statement;
        }
    *jis case mei function void type ka ho usmeii return likhne ki need nahi
    *pr agar ham koi function bnate hai toh main function mei call k liye
        functionname();
    *-*Syntax for Parameters(i.e., input)=
        returnType name(type param1,type 2 param2){
            //body
            return statement;
        }
    **Parameters mtlb woh value jisko function k andar call kri jaye
    **Arguments mtlb woh value jisko main function mei call kri jaye
    **types of Parameters:
        formal Parameters(definition k andar)
        actual Parameters or arguments(call)
    **'CALLSTACK'=it track calling of fx
    **Memory takes fx mei stack k form mei

-CALL BY VALUE:
    swap=interchange
    int a=5;
    int b=10;
    int temp=a;
    a=b;
    b=temp;

-Methods
    user defined =factorial,sum,product
    inbuilt method=pow,sqrt,max

-FUNCTION OVERLOADING
    differentt fx k same naam rakh skte
    mutiple fx with the sme name but different Parameters
    *fx overloading not works on return type


-*ARRAYS
    0-BASE Indexing (thats why we use 0 to 49 as 50 values.)
    Definition :list of elements of the same type placed in a contiguius memory location
    *Operations in ARRAYS
        create,input,output,update

-*Linear Search
    we can aslo print in the arrays for print like
        Arrrays.binarySearch(arr,value)
        Arrays.toString()
        Arrays.equal(arr1,arr2)
        Arrays.sort(arr)
    #  -infinity=Integer.MIN_VALUE
    #  +infinity=Integer.MAX_VALUE

=*Binary Search
    (pesudo code)=
        start=0,end=n-1
        while(start<=end)
            find mid
            compare mid and key
                mid==key FOUND
                mid>key LEFT
                mid<key  RIGHT


*"Java: Clean Java Language Server Workspace
for package

**prefix[end]-prefix[start-1]
prefix[i-1]+arr[i]
subarray 