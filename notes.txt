



-ARRRAY
    list of elements of same type placed in a contigous memory location
    -OPERATIONS ON array
        Create,input,output and update
    *SYNTAX:
        datatypes arrayName[]=new dataType[size];
            //creation of array
            int marks[]=new int[50];
            int numbers[]={};
            String fruits[]={'','',''};
    **arrayname.length
        system.out.println("length of array"+arrayname.length)
        
Function overloading
    ham naam same rakh skte pr values alag alag
    it depends on values not returnType

-*for squareroot we write
    math.sqrt(n)  //mathy is class and sqrt is fx

-BINARY TO DECIMAL
decimal=decimal+(lastdigit*2^pow)
**for power=Math.pow(base,pow)


-Scope Methods
    METHOD SCOPE 
    BLOCK SCOPE
    CLASS SCOPE

*CDS
AFCAt
IBPS SO-IT officer